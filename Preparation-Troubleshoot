chat-ASPICE-Ai studio -project structure 
(folder structure->aws->maven->jenkins(ci/cd)->terraform(IAM)->docker(image)/container-k8)

Health Insurance Portability and Accountability Act (HIPAA)-project ->project structure 
(folder structure->aws->maven->jenkins(ci/cd)->terraform(IAM)->docker(image)/container-k8)
.....................................
scripting language-automation
python
bash acript
..................
UT/IT- test spec- automation -ci/cd process->githubaction or jenkins

...................
Resume -edit- 50% 
  ->project 
  ->technical skill
..................
experince letter /document check
youtube-devops interview Q/A-https://youtu.be/GX6fOvaS0Xs
youtube- dockers-k8s- https://youtu.be/2bg9MAtiHwo
........................
browse-> naukri
      ->linkedin
      ->devops group
............
daily command
ps top ssh free htop crontab -e alias ping google.com
ifconfig ipconfig sudo apt update 
..........
unit test
after python installed->virtual env- source venv/bin/activate  
run -> py.test <file_name> 
show result -> py.test -k <module_name> -v -s 
.....................
facing issue/trouble-shoot 
1)in HIPAA project->after install java,mvn,tomcat -mvn build is done generate .war file after check web portal-401 eror 
 ->we did mvn clean package-rm old file from webapp and again copy new .war file and webapp-tomcat server path -restart tomcat10
server 
2)mvn clean packege-flow make as bash script using .sh ->here issue -pom.xml outside the directory .sh-to change in bash script,
 -ad outside pom.xml path in bash script <~cd>
3)mvn automation bash script run effect on -tomcat server -buil time - tomcat server getting fail - to run cmd <sudo journalctl -xeu tomcat10.service> get exact issue -some library 
deleted -- so uninstall tomcat server cmd- sudo systemctl stop tomcat10
sudo apt purge tomcat10 -y
sudo apt autoremove -y
sudo rm -rf /usr/share/tomcat10 /etc/tomcat10 /var/lib/tomcat10 /usr/libexec/tomcat10 -agin re-install cmd <sudo apt install tomcat10 -y
4)Unit test-python py.test job build in jenkins run- jenkins api-token authentication issue -not resloved
5)unit test- python py.test- githubaction in github -to write the the groovey script in github- clone into aws-server
 -after changes in sc- git status-commit-git push origin- goto-githubaction run the job-succeed -automation done
*1)6)401 error -> inside webapp/ no WEB-INF folder web.xml absent so this error - after adding source code web.xml and 
index.jsp outside the webapp ->mv to webapp folder -error reslove
7)after web-port run -get 501 error- this java version mismatched , old java jdk17 - need to indstall java21 -with upgrade
same congigure -<sudo update-alternatives --config java> <restart srerver> - error resloved
8)if localy we will check webportal work or not rum cmd curl with url -<curl http://35.95.17.36:8080/hipaa-demo/>
to show -what in index.jsp contetnt - if this file empty - should add source code 
9) if mvn clean stage - server file deleted- manual we shold add -create directory 
if webapps path is deleted -re create
# Recreate the webapps directory
sudo mkdir -p /var/lib/tomcat10/webapps

# Set correct ownership (Tomcat usually runs as 'tomcat' user)
sudo chown -R tomcat:tomcat /var/lib/tomcat10/webapps

# Copy your WAR file again
sudo cp /home/ubuntu/target/hipaa-demo.war /var/lib/tomcat10/webapps/
# Restart Tomcat

10) terraform installtion +githubaction -jobbuild error -once terraform folder structure is done after -init terraform &
-apply terraform - once github groovy script done -run the githubaction
*11) docker file configuration->script need .war and java version
  script-
FROM tomcat:10.1-jdk21-openjdk
COPY target/hipaa-demo.war /usr/local/tomcat/webapps/ROOT.war
EXPOSE 8080
CMD ["catalina.sh","run"]

*12)-java version chnage -> change the dockerfile (version) = run docker file -cmd <docker build -t hipaa-tomcat10 .>
getting disc-space issue -rem temp file 
-Create in EC2 - volume → Actions → Attach volume.
-Choose your instance ID and device name -once vloume added 
need to Move Docker Storage to /data (/data is the new volum)
-Stop Docker service <sudo systemctl stop docker>
Move Docker data <sudo mv /mnt/docker /data/docker>
Create symlink <sudo ln -s /data/docker /mnt/docker>
Restart Docker <sudo systemctl start docker>
Verify new root directory <docker info | grep "Docker Root Dir">
It should now show /data/docker.
.............
Permanent Fix: Move containerd storage -we create continer for this issue
Stop Docker service <sudo systemctl stop docker>
Move containerd data to /data  <sudo mv /var/lib/containerd /data/containerd>
<sudo ln -s /data/containerd /var/lib/containerd>
Restart Docker <sudo systemctl start docker>
Verify space usage <df -h>
You should now see /data handling both Docker and containerd storage.

13)Rebuild image <docker build -t hipaa-tomcat10 .>
-once docker build images then we will run <docker run -d -p 8081:8080 hipaa-tomcat10>
14)docker conatiner with port 8080 if alreday used 8080
you can use 8081 -add inbond rule 
then after EC2 instance public_ip<8081> check in web-application will present
...
14)k8s we will do k8s configuration
need to write 3 yaml script -namespace.yaml /-deployment.yaml /-service.yaml
kubectl --classic installation and minikube installtion 
less space error getting 
move to minikude to /data
1st minikube delete then after we make one directory for minikube <sudo mkdir -p /data/minikube>
give ownership <sudo chown $USER:$USER /data/minikube>
<minikube start --driver=docker --root=/data/minikube --disk-size=10g --memory=2048>
<export MINIKUBE_HOME=/data/minikube>
15)<minikube start --driver=docker --disk-size=10g --memory=2048>
then check kubernites info- created or not <kubectl cluster-info>
check nod <kubectl get nodes>
run cmd for 3 yaml script 
<kubectl apply -f namespace.yaml>
<kubectl apply -f deployment.yaml>
<kubectl apply -f service.yaml>
-verify the pods-
............................
kubectl get ns
kubectl get pods -n hipaa-project
kubectl get svc -n hipaa-project
...............
16)Error: Failed to fetch http://us-west-2.ec2.archive.ubuntu.com/ubuntu/dists/resolute-updates/main/binary-amd64v3/Packages  404  Not Found [IP: 34.210.25.51 80]
--is either temporarily unavailable or misconfigured. 
It’s not a space issue anymore — it’s a repository problem.
=error the error in your screenshot means your workflow is using an outdated version of the actions/upload-artifact action.
GitHub deprecated v3 in April 2024, so any workflow referencing it will fail automatically.
.......
unit-test-python 
install python -after install env = <apt install python3.14-venv> next <python3 -m venv venv>
-run <source venv/bin/activate> python-virtul env open
sudo apt install python3-pytest = install pytest for run pytest test case
error -The terminal output shows that pytest failed to collect tests from test_api.py because Python couldn’t find the module hipaa_demo.
-git pull origin main --rebase

Your repo should look like this:

Code
pytest_demo/
 ├── hipaa_demo/
 │    ├── __init__.py
 │    ├── api.py
 │    ├── auth.py
 │    ├── audit.py
 │    └── crypto.py
 ├── tests/
 │    ├── test_api.py
 │    ├── test_auth.py
 │    ├── test_audit.py
 │    └── test_crypto.py
 ├── requirements.txt
 └── .github/workflows/python-tests.yml
then do <git push orgin >
once done - goto github action run the job ->automation done ci/cd 
